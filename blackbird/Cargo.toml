[workspace]
members = ["engine", "wasm", "component"]
resolver = "2"

[profile.release]
# The web build ships over the wire: trade compile time for size.
opt-level = 3
lto = true
codegen-units = 1
strip = true
