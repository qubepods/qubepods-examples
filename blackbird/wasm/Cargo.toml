[package]
name = "blackbird-wasm"
version = "0.1.0"
edition = "2021"
description = "bb_* web API over the chess-engine core — the wasm module's entire surface"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
chess-engine = { path = "../engine" }
