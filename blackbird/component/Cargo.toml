[package]
name = "blackbird-component"
version = "0.1.0"
edition = "2021"
description = "WIT component (qubepods:blackbird/blackbird) over the chess-engine core"

[lib]
crate-type = ["cdylib"]

[dependencies]
chess-engine = { path = "../engine" }
wit-bindgen = "0.58"
